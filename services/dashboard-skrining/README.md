# Dashboard Skrining (SIKERJA)

Repo: `../../../dashboard-skrining` — env: `dashboard-skrining/.env`

Penamaan database: prefix **`sikerja`** (bukan `mcu_monitor`).

| Item | Nilai |
|------|--------|
| Host | `sikerja-postgres` |
| Database | `sikerja_ppkp` |
| User | `sikerja` |
| Password | `DASHBOARD_DB_PASSWORD` |

Port: **http://127.0.0.1:9006**
